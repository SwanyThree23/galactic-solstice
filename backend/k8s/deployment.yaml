apiVersion: apps/v1
kind: Deployment
metadata:
  name: seewhy-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: seewhy-backend
  template:
    metadata:
      labels:
        app: seewhy-backend
    spec:
      containers:
      - name: backend
        image: seewhy-backend:latest
        ports:
        - containerPort: 4000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: seewhy-secrets
              key: database-url
